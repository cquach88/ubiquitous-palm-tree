{
  "name": "tu-sac",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Bài Tứ Sắc (Four-Color Cards) — web game with a portable, dependency-free core engine",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "peer": "^1.0.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "peerjs": "^1.5.5"
  }
}
